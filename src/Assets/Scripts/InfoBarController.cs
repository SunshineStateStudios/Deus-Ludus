using UnityEngine;

public class InfoBarCotroller : MonoBehaviour
{
    public GameObject infoBar;
    void Update()
    {
        /*Vector3 mousePosition = Input.mousePosition;
        mousePosition.z = 10f; 
        infoBar.transform.position = Camera.main.ScreenToWorldPoint(mousePosition);*/
    }
}
